---
title: 'Unconditional Image Generation'
icon: 'image'
description:
  Generate images without specific conditions or inputs for applications in art generation, creative
  design, and data augmentation
mode: 'wide'
---

<Accordion defaultOpen title="Basic usage">
  <CodeGroup>
    ```javascript javascript
    import Bytez from 'bytez.js'

    const sdk = new Bytez("BYTEZ_KEY");
    const model = sdk.model("afshr/cam_finetune");

    await model.create()

    const { error, output } = await model.run();

    console.log({ error, output })
    ```
    ```python python
    from bytez import Bytez

    sdk = Bytez("BYTEZ_KEY")
    model = sdk.model("afshr/cam_finetune")

    model.create()

    output, error = model.run()

    print({ "error": error, "output": output })
    ```
    ```bash http
    curl -X POST 'https://api.bytez.com/models/v2/afshr/cam_finetune' \
    -H 'Authorization: Key BYTEZ_KEY' \
    -H 'Content-Type: application/json'
    ```

  </CodeGroup>
</Accordion>
