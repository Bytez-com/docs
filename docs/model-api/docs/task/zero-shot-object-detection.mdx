---
title: 'Zero Shot Object Detection'
icon: 'scanner-image'
description:
  Detect objects in images without prior training on those specific objects. Use cases include novel
  object detection, transfer learning, and few-shot learning
mode: 'wide'
---

import Base64 from '/snippets/base64.mdx';

<Accordion defaultOpen title="Basic usage">
  Send an image URL and a set of candidate labels to detect objects in the image:

  <CodeGroup>
    ```javascript javascript
    import Bytez from 'bytez.js'

    const sdk = new Bytez("BYTEZ_KEY");
    const model = sdk.model("google/owlv2-base-patch16-finetuned");

    await model.create()

    const { error, output } = await model.run({
      url: "https://ocean.si.edu/sites/default/files/styles/3_2_largest/public/2023-11/Screen_Shot_2018-04-16_at_1_42_56_PM.png.webp?itok=Icvi-ek9",
      candidate_labels: ["squid", "octopus", "human", "cat"]
    });

    console.log({ error, output })
    ```
    ```python python
    from bytez import Bytez

    sdk = Bytez("BYTEZ_KEY")
    model = sdk.model("google/owlv2-base-patch16-finetuned")

    model.create()

    output, error = model.run({
      "url": "https://ocean.si.edu/sites/default/files/styles/3_2_largest/public/2023-11/Screen_Shot_2018-04-16_at_1_42_56_PM.png.webp?itok=Icvi-ek9",
      "candidate_labels": ["squid", "octopus", "human", "cat"]
    })

    print({ "error": error, "output": output })
    ```
    ```bash http
    curl -X POST 'https://api.bytez.com/models/v2/google/owlv2-base-patch16-finetuned' \
    -H 'Authorization: Key BYTEZ_KEY' \
    -H 'Content-Type: application/json' \
    --data '{
      "url": "https://ocean.si.edu/sites/default/files/styles/3_2_largest/public/2023-11/Screen_Shot_2018-04-16_at_1_42_56_PM.png.webp?itok=Icvi-ek9",
      "candidate_labels": ["squid", "octopus", "human", "cat"]
    }'
    ```

  </CodeGroup>
  <Base64 type='image'/>
</Accordion>
