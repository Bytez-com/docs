---
title: 'text-classification'
icon: 'input-text'
description:
  Categorize text into predefined classes for applications like sentiment analysis, spam detection,
  and topic classification
mode: 'wide'
---

<Accordion defaultOpen title="Basic usage">
  Example, sentiment analysis:

  <CodeGroup>
    ```javascript javascript
    import Bytez from 'bytez.js'

    const sdk = new Bytez("BYTEZ_KEY");
    const model = sdk.model("AdamCodd/distilbert-base-uncased-finetuned-sentiment-amazon");

    await model.create()

    const { error, output } = await model.run("We are furious with the results of the experiment!");

    console.log({ error, output })
    ```
    ```python python
    from bytez import Bytez

    sdk = Bytez("BYTEZ_KEY")
    model = sdk.model("AdamCodd/distilbert-base-uncased-finetuned-sentiment-amazon")

    model.create()

    output, error = model.run("We are furious with the results of the experiment!")

    print({ "error": error, "output": output })
    ```
    ```bash http
    curl -X POST 'https://api.bytez.com/models/v2/AdamCodd/distilbert-base-uncased-finetuned-sentiment-amazon' \
    -H 'Authorization: Key BYTEZ_KEY' \
    -H 'Content-Type: application/json' \
    --data '{
      "text": "We are furious with the results of the experiment!"
    }'
    ```

  </CodeGroup>
</Accordion>
