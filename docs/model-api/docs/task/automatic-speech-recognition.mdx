---
title: 'Automatic Speech Recognition'
icon: 'file-music'
description:
  Convert spoken language into written text for transcription services, voice assistants, and
  accessibility features.
mode: 'wide'
---

import Base64 from '/snippets/base64.mdx';

<Accordion defaultOpen title="Basic usage">
  Send an audio file to an ASR model to generate text.

  <CodeGroup>
    ```javascript javascript
    import Bytez from 'bytez.js'

    const sdk = new Bytez("BYTEZ_KEY");
    const model = sdk.model("facebook/data2vec-audio-base-960h");

    await model.create()

    const audio = "https://huggingface.co/datasets/huggingfacejs/tasks/resolve/main/automatic-speech-recognition/input.flac"
    const { error, output } = await model.run({ url: audio });

    console.log({ error, output })
    ```
    ```python python
    from bytez import Bytez

    sdk = Bytez("BYTEZ_KEY")
    model = sdk.model("facebook/data2vec-audio-base-960h")

    model.create()

    audio = "https://huggingface.co/datasets/huggingfacejs/tasks/resolve/main/automatic-speech-recognition/input.flac"
    output, error = model.run({ "url": audio })

    print({ "error": error, "output": output })
    ```
    ```bash http
    curl -X POST 'https://api.bytez.com/models/v2/facebook/data2vec-audio-base-960h' \
    -H 'Authorization: Key BYTEZ_KEY' \
    -H 'Content-Type: application/json' \
    --data '{
      "url": "https://huggingface.co/datasets/huggingfacejs/tasks/resolve/main/automatic-speech-recognition/input.flac"
    }'
    ```

  </CodeGroup>
  <Base64 type="audio" />

</Accordion>
